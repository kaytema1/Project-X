<%= form_with(model: clothing_line, local: true) do |form| %>
  <% if clothing_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clothing_line.errors.count, "error") %> prohibited this clothing_line from being saved:</h2>

      <ul>
      <% clothing_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :tag_line %>
    <%= form.text_area :tag_line %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
